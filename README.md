# CRUDFranciscoAMP
Proyecto CRUD: Create, Read, Unifique, Delete. Pasado por Jesús y que debemos forzar errores mostrando páginas propias mosgrando los errores.

## Proyecto.
crear una pagina para mostrar los errores tipicos de sql.

Proyecto CRUD: Create, Read, Unifique, Delete. Pasado por Jesús y que debemos forzar errores mostrando páginas propias mosgrando los errores.

Forzar error de sintaxis dentro de la aplicacion que nos ha pasado, para sacar una pagina de error creada por nosotros y se le pasa por peticion el error.


getCode ->  para forzar 
~~~
select fro ave
~~~
Sacar el mensaje de error 500 de mysql. El error no se lanza desde un servlet.

Vamos a crear otro paquete llamado **exceptions** y una clase llamada *mensajeError.java*.

Al metodo le metemos el codigo de error y nos devuelve el mensaje.

Y que se pueda volver a ***eliminar***, en el caso de ***eliminar***.

